production: &default
  max_size: 100_000  # Die maximale Anzahl an Einträgen die im RAM gehalten werden soll.
  save_to_disk: YES  # Sollen überschüssige Einträge und alle Operationen niedergeschrieben werden.

development:
  <<: *default

test:
  <<: *default
